import { SquareKilometerPipe } from './square-kilometer.pipe';

describe('SquareKilometerPipe', () => {
  it('create an instance', () => {
    const pipe = new SquareKilometerPipe();
    expect(pipe).toBeTruthy();
  });
});
